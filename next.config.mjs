/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow unoptimized images (local public/ assets served as-is)
    unoptimized: true,
  },
};

export default nextConfig;
